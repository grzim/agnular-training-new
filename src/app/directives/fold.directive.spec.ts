import { FoldDirective } from './fold.directive';

describe('FoldDirective', () => {
  it('should create an instance', () => {
    const directive = new FoldDirective();
    expect(directive).toBeTruthy();
  });
});
