import { LogPropsDirective } from './log-props.directive';

describe('LogPropsDirective', () => {
  it('should create an instance', () => {
    const directive = new LogPropsDirective();
    expect(directive).toBeTruthy();
  });
});
